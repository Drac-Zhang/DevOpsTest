# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:
- task: AzureCLI@1
  inputs:
    azureSubscription: 'yuzhang6-Microsoft Azure internal Consumption(aafb27de-e728-4cb8-9e68-9afde1d1a849)'
    scriptLocation: 'inlineScript'
    inlineScript: 'az functionapp config appsettings set --name DracLogicAppStandard --resource-group LogicAppStandard --settings "BLOB_CONNECTION_RUNTIMEURL=Test"'
